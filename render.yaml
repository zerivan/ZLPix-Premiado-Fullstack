services:
  - type: web
    name: zlpix-backend
    env: node
    region: oregon
    rootDir: backend
    buildCommand: npm install && npm run migrate:force && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20