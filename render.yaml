services:
  - type: web
    name: zlpix-backend
    env: node
    region: oregon
    rootDir: backend
    buildCommand: npm ci && npx prisma generate || echo "skip prisma" && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20